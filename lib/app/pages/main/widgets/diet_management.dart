import 'package:flutter/material.dart';

class DietManagement extends StatelessWidget {
  const DietManagement({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Diet Management');
  }
}
